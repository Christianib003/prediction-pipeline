plant-disease-db-api/
├── .gitignore             # Tells Git which files to ignore (e.g., __pycache__, .env)
├── README.md              # Project description, setup, and run instructions
├── requirements.txt       # List of all Python packages needed (FastAPI, Uvicorn, etc.)
├── .env.example           # A template for environment variables (DB passwords, etc.)
│
├── api/                   # All code for the FastAPI application
│   ├── main.py            # Entry point to start the API server
│   ├── app/
│   │   ├── __init__.py
│   │   ├── routes.py      # Defines the API endpoints (/images/latest, etc.)
│   │   ├── models.py      # Pydantic models for request/response data
│   │   ├── services.py    # Functions that handle the business logic (querying the DB)
│   │   └── db_connector.py# Manages connections to SQL and MongoDB
│
├── database/              # Scripts for database setup and management
│   ├── schema.sql         # The CREATE TABLE statements for your SQL database
│   ├── advanced.sql       # The Stored Procedure and Trigger definitions
│   └── populate_db.py     # A script to read the dataset and fill the databases
│
├── prediction/            # The machine learning script and related files
│   ├── predict.py         # The script that calls the API and runs the model
│   └── model/
│       └── plant_classifier.h5 # Your saved ML model file (or .pkl, etc.)
│
└── data/                  # For the dataset itself
    └── README.md          # IMPORTANT: Explains where to download the dataset from